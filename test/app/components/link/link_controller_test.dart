import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:site/app/components/link/link_controller.dart';
import 'package:site/app/app_module.dart';

void main() {
  initModule(AppModule());
  // LinkController link;
  //
  setUp(() {
    //     link = AppModule.to.get<LinkController>();
  });

  group('LinkController Test', () {
    //   test("First Test", () {
    //     expect(link, isInstanceOf<LinkController>());
    //   });

    //   test("Set Value", () {
    //     expect(link.value, equals(0));
    //     link.increment();
    //     expect(link.value, equals(1));
    //   });
  });
}
