import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:site/app/components/line/line_controller.dart';
import 'package:site/app/app_module.dart';

void main() {
  initModule(AppModule());
  // LineController line;
  //
  setUp(() {
    //     line = AppModule.to.get<LineController>();
  });

  group('LineController Test', () {
    //   test("First Test", () {
    //     expect(line, isInstanceOf<LineController>());
    //   });

    //   test("Set Value", () {
    //     expect(line.value, equals(0));
    //     line.increment();
    //     expect(line.value, equals(1));
    //   });
  });
}
